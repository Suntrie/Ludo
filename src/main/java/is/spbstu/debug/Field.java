package is.spbstu.debug;

import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

public class Field {
    //private List<Peg> pegs = new ArrayList<>();
/*
    //public boolean isBlocked(){
        return pegs.size()==2;
    }*/

/*    public Optional<Peg> setPeg(Peg peg){
        if (isBlocked()){
            throw new IllegalStateException("isBlocked() should be called before");
        };

        Peg removedPeg = null;

        if (!pegs.isEmpty()){
            removedPeg = pegs.remove(0);
            pegs.add(peg);
        }

        return Optional.ofNullable(removedPeg);
    }*/
}
